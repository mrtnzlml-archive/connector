language: php
sudo: required
dist: trusty
group: edge
php:
  - hhvm-3.12
  - hhvm-nightly

before_script:
  - travis_retry composer install --prefer-dist

script:
  - vendor/bin/run-tests tests -p hhvm -s

notifications:
  email: false
